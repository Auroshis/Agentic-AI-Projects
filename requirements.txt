fastmcp

langchain
langchain-community
langchain-core
langchain-openai
langchain-text-splitters

langgraph
langgraph-checkpoint
langgraph-prebuilt
langgraph-sdk

langchain-mcp-adapters

langsmith

# ── DeepJobAgent dependencies ─────────────────────────────────────────────────
# Google Docs API
google-api-python-client>=2.150.0
google-auth>=2.37.0
google-auth-httplib2>=0.2.0

# LinkedIn scraping (Chrome TLS impersonation)
curl-cffi>=0.7.0
beautifulsoup4>=4.12.0

# Async HTTP (LeetCode GraphQL, fallback fetches)
httpx>=0.27.0

# Environment
python-dotenv>=1.0.0

# Type helpers
typing-extensions>=4.12.0

# Optional: Playwright for LinkedIn (run: playwright install chromium after pip install)
# playwright>=1.45.0

# PDF parsing
pypdf>=4.0.0

# ── API server ────────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9